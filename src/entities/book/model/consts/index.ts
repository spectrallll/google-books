export enum BookSortField {
  RELEVANCE = "relevance",
  NEWEST = "newest"
}

export enum BookCategoryField {
  ALL = "all",
  ART = "art",
  BIOGRAPHY = "biography",
  COMPUTERS = "computers",
  HISTORY = "history",
  MEDICAL = "medical",
  POETRY = "poetry"
}